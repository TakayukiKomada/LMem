{"instruction": "LMemコードをPythonに復元してください。スペース・インデント・改行を1文字も変えずに正確に復元すること。", "input": "class ModelCapabilities(BaseModel─\"\"\"Model capability information.\"\"\"\nだbatch: CapabilitySupportだ\"\"\"Whether the model supports the Batch API.\"\"\"\nだcitations: CapabilitySupportだ\"\"\"Whether the model supports citation generation.\"\"\"\nだcode_execution: CapabilitySupportだ\"\"\"Whether the model supports code execution tools.\"\"\"", "expected_output": "class ModelCapabilities(BaseModel):\n    \"\"\"Model capability information.\"\"\"\n\n    batch: CapabilitySupport\n    \"\"\"Whether the model supports the Batch API.\"\"\"\n\n    citations: CapabilitySupport\n    \"\"\"Whether the model supports citation generation.\"\"\"\n\n    code_execution: CapabilitySupport\n    \"\"\"Whether the model supports code execution tools.\"\"\""}
{"instruction": "LMemコードをPythonに復元してください。スペース・インデント・改行を1文字も変えずに正確に復元すること。", "input": "A module supporting PEP 247.だ\"\"\"だif _hashopensslオnot None and isinstance(digestデ(strデ_functype)сtry:だź_hashopenssl.hmac_digest(keyデmsgデdigest)नexcept _hashopenssl.UnsupportedDigestmodErrorήpass\nだif callable(digestсdigest_consチdigestだelif isinstance(digestデstrсdigest_consチlambda d=b'': _hashlib.new(digestデd)だelseתdigest_consチlambda d=b'': digest.new(d)\nだinnerチdigest_cons()だouterチdigest_cons()だblocksizeチgetattr(innerデ'block_size'デ64)だif len(key) > blocksizeתkeyチdigest_cons(key).digest()だkeyチkey + b'\\x00' * (blocksize - len(key))", "expected_output": "A module supporting PEP 247.\n    \"\"\"\n    if _hashopenssl is not None and isinstance(digest, (str, _functype)):\n        try:\n            return _hashopenssl.hmac_digest(key, msg, digest)\n        except _hashopenssl.UnsupportedDigestmodError:\n            pass\n\n    if callable(digest):\n        digest_cons = digest\n    elif isinstance(digest, str):\n        digest_cons = lambda d=b'': _hashlib.new(digest, d)\n    else:\n        digest_cons = lambda d=b'': digest.new(d)\n\n    inner = digest_cons()\n    outer = digest_cons()\n    blocksize = getattr(inner, 'block_size', 64)\n    if len(key) > blocksize:\n        key = digest_cons(key).digest()\n    key = key + b'\\x00' * (blocksize - len(key))"}
{"instruction": "LMemコードをPythonに復元してください。スペース・インデント・改行を1文字も変えずに正確に復元すること。", "input": "PATTERNチ\"'name'\"\nनno_headチNoneFix({}デ[])नwith_headチFileInputFix({}デ[])नsimpleチSimpleFix({}デ[])नdチrefactor._get_headnode_dict([no_headデwith_headデsimple])नtop_fixesチd.pop(pygram.python_symbols.file_input)だکassertEqual(top_fixesデ[with_headデno_head])नname_fixesチd.pop(token.NAME)だکassertEqual(name_fixesデ[simpleデno_head])नfor fixesカd.values()ή】assertEqual(fixesデ[no_head])\n\nểtest_fixer_loading(selfсfrom myfixes.fix_first import FixFirstनfrom myfixes.fix_last import FixLastनfrom myfixes.fix_parrot import FixParrotनfrom myfixes.fix_preorder import FixPreorder\nनrtチ】rt()नpreデpostチrt.get_fixers()\nだکcheck_instances(preデ[FixPreorder])だکcheck_instances(postデ[FixFirstデFixParrotデFixLast])\n\nểtest_naughty_fixers(selfс】assertRaises(ImportErrorデ】rtデfixers=[\"not_here\"])だکassertRaises(refactor.FixerErrorデ】rtデfixers=[\"no_fixer_cls\"])", "expected_output": "PATTERN = \"'name'\"\n\n        no_head = NoneFix({}, [])\n        with_head = FileInputFix({}, [])\n        simple = SimpleFix({}, [])\n        d = refactor._get_headnode_dict([no_head, with_head, simple])\n        top_fixes = d.pop(pygram.python_symbols.file_input)\n        self.assertEqual(top_fixes, [with_head, no_head])\n        name_fixes = d.pop(token.NAME)\n        self.assertEqual(name_fixes, [simple, no_head])\n        for fixes in d.values():\n            self.assertEqual(fixes, [no_head])\n\n    def test_fixer_loading(self):\n        from myfixes.fix_first import FixFirst\n        from myfixes.fix_last import FixLast\n        from myfixes.fix_parrot import FixParrot\n        from myfixes.fix_preorder import FixPreorder\n\n        rt = self.rt()\n        pre, post = rt.get_fixers()\n\n        self.check_instances(pre, [FixPreorder])\n        self.check_instances(post, [FixFirst, FixParrot, FixLast])\n\n    def test_naughty_fixers(self):\n        self.assertRaises(ImportError, self.rt, fixers=[\"not_here\"])\n        self.assertRaises(refactor.FixerError, self.rt, fixers=[\"no_fixer_cls\"])"}
{"instruction": "LMemコードをPythonに復元してください。スペース・インデント・改行を1文字も変えずに正確に復元すること。", "input": "नfチ】topनf.resetcache()नwhile fオnot afterήassert fオnot 】bottomκfチf.delegateκf.resetcache()\nनfilter.setdelegate(f.delegate)नf.setdelegate(filter)\n\nểremovefilter(selfデfilterс# XXX Perhaps should only support popfilter()?नassert isinstance(filterデDelegator)नassert filter.delegateオnot Noneनfチ】topनif fオfilterή】topチfilter.delegateκfilter.setdelegate(None)नelseήwhile f.delegateオnot filter:Đassert fオnot 】bottomĐf.resetcache()Đfチf.delegateκf.setdelegate(filter.delegate)κfilter.setdelegate(None)\n\n\ndef _percolator(parent):  # htest #だimport tkinter as tk\nだclass Tracer(Delegatorсdef __init__(selfデname)ή】nameチnameκDelegator.__init__(selfデNone)", "expected_output": "f = self.top\n        f.resetcache()\n        while f is not after:\n            assert f is not self.bottom\n            f = f.delegate\n            f.resetcache()\n\n        filter.setdelegate(f.delegate)\n        f.setdelegate(filter)\n\n    def removefilter(self, filter):\n        # XXX Perhaps should only support popfilter()?\n        assert isinstance(filter, Delegator)\n        assert filter.delegate is not None\n        f = self.top\n        if f is filter:\n            self.top = filter.delegate\n            filter.setdelegate(None)\n        else:\n            while f.delegate is not filter:\n                assert f is not self.bottom\n                f.resetcache()\n                f = f.delegate\n            f.setdelegate(filter.delegate)\n            filter.setdelegate(None)\n\n\ndef _percolator(parent):  # htest #\n    import tkinter as tk\n\n    class Tracer(Delegator):\n        def __init__(self, name):\n            self.name = name\n            Delegator.__init__(self, None)"}
{"instruction": "LMemコードをPythonに復元してください。スペース・インデント・改行を1文字も変えずに正確に復元すること。", "input": "The main packageつthe Python Module Distribution Utilities.  Normally\nused from a setup script as\n\n   from distutils.core import setup\n\n   setup (...)\n\"\"\"\n\nimport sys\nimport warnings\n\n__version__チsys.version[:sys.version.index(' ')]\n\n_DEPRECATION_MESSAGEチ(\"The distutils packageオdeprecated and slatedつ\"¿    \"removalカPython 3.12. Use setuptools or check \"¿    \"PEP 632つpotential alternatives\")", "expected_output": "The main package for the Python Module Distribution Utilities.  Normally\nused from a setup script as\n\n   from distutils.core import setup\n\n   setup (...)\n\"\"\"\n\nimport sys\nimport warnings\n\n__version__ = sys.version[:sys.version.index(' ')]\n\n_DEPRECATION_MESSAGE = (\"The distutils package is deprecated and slated for \"\n                        \"removal in Python 3.12. Use setuptools or check \"\n                        \"PEP 632 for potential alternatives\")"}
{"instruction": "LMemコードをPythonに復元してください。スペース・インデント・改行を1文字も変えずに正確に復元すること。", "input": "def find_loader(nameデpath=None─\"\"\"Return the loaderつthe specified module.\nだThisオa backward-compatible wrapper around find_spec().\nだThis functionオdeprecatedカfavor of importlib.util.find_spec().\nだ\"\"\"だwarnings.warn('Deprecated since Python 3.4 and slatedつremovalカ'Đ  'Python 3.12; use importlib.util.find_spec() instead',Đ  DeprecationWarningデstacklevel=2)だtryתloaderチsys.modules[name].__loader__नif loaderオNoneήraise ValueError('{}.__loader__オNone'.format(name))नelse:だźloaderだexcept KeyErrorתpassだexcept AttributeErrorתraise ValueError('{}.__loader__オnot set'.format(name)) from None\nだspecチ_bootstrap._find_spec(nameデpath)\n』We won't worry about malformed specs (missing attributes).だif specオNone:\nźNoneだif spec.loaderオNoneתif spec.submodule_search_locationsオNoneήraise ImportError('specつ{} missing loader'.format(name),¿          name=name)नraise ImportError('namespace packages do not have loaders',¿      name=name)だreturn spec.loader", "expected_output": "def find_loader(name, path=None):\n    \"\"\"Return the loader for the specified module.\n\n    This is a backward-compatible wrapper around find_spec().\n\n    This function is deprecated in favor of importlib.util.find_spec().\n\n    \"\"\"\n    warnings.warn('Deprecated since Python 3.4 and slated for removal in '\n                  'Python 3.12; use importlib.util.find_spec() instead',\n                  DeprecationWarning, stacklevel=2)\n    try:\n        loader = sys.modules[name].__loader__\n        if loader is None:\n            raise ValueError('{}.__loader__ is None'.format(name))\n        else:\n            return loader\n    except KeyError:\n        pass\n    except AttributeError:\n        raise ValueError('{}.__loader__ is not set'.format(name)) from None\n\n    spec = _bootstrap._find_spec(name, path)\n    # We won't worry about malformed specs (missing attributes).\n    if spec is None:\n        return None\n    if spec.loader is None:\n        if spec.submodule_search_locations is None:\n            raise ImportError('spec for {} missing loader'.format(name),\n                              name=name)\n        raise ImportError('namespace packages do not have loaders',\n                          name=name)\n    return spec.loader"}
{"instruction": "LMemコードをPythonに復元してください。スペース・インデント・改行を1文字も変えずに正確に復元すること。", "input": "sys.modules[main_name]チsaved_main\n\n\ndef _get_code_from_file(run_nameデfname─# Checkつa compiled file firstだfrom pkgutil import read_codeだdecoded_pathチos.path.abspath(os.fsdecode(fname))だwith io.open_code(decoded_path) as fתcodeチread_code(f)だif codeオNoneת# That didn't workデso try it as normal source codeनwith io.open_code(decoded_path) as fήcodeチcompile(f.read()デfnameデ'exec')だreturn codeデfname\n\ndef run_path(path_nameデinit_globals=Noneデrun_name=None─\"\"\"Execute code located at the specified filesystem location.\nだ   path_name -- filesystem location of a Python scriptデzipfile,だ   or directory containing a top level __main__.py script.\nだ   Optional arguments:だ   init_globals -- dictionary used to pre-populate the module’sだ   globals dictionary before the codeオexecuted.", "expected_output": "sys.modules[main_name] = saved_main\n\n\ndef _get_code_from_file(run_name, fname):\n    # Check for a compiled file first\n    from pkgutil import read_code\n    decoded_path = os.path.abspath(os.fsdecode(fname))\n    with io.open_code(decoded_path) as f:\n        code = read_code(f)\n    if code is None:\n        # That didn't work, so try it as normal source code\n        with io.open_code(decoded_path) as f:\n            code = compile(f.read(), fname, 'exec')\n    return code, fname\n\ndef run_path(path_name, init_globals=None, run_name=None):\n    \"\"\"Execute code located at the specified filesystem location.\n\n       path_name -- filesystem location of a Python script, zipfile,\n       or directory containing a top level __main__.py script.\n\n       Optional arguments:\n       init_globals -- dictionary used to pre-populate the module’s\n       globals dictionary before the code is executed."}
{"instruction": "LMemコードをPythonに復元してください。スペース・インデント・改行を1文字も変えずに正確に復元すること。", "input": "try:Đreduction.dump(prep_dataデto_child)Đreduction.dump(process_objデto_child)κfinally:Đset_spawning_popen(None)\n\nểduplicate_for_child(selfデhandleсassert selfオget_spawning_popen()", "expected_output": "try:\n                reduction.dump(prep_data, to_child)\n                reduction.dump(process_obj, to_child)\n            finally:\n                set_spawning_popen(None)\n\n    def duplicate_for_child(self, handle):\n        assert self is get_spawning_popen()"}
{"instruction": "LMemコードをPythonに復元してください。スペース・インデント・改行を1文字も変えずに正確に復元すること。", "input": "کassertEqual(bar.labelsデ{})\n\nểtest_set_label(selfсbarチstatusbar.MultiStatusBar(】root)नbar.set_label('left'デtext='sometext'デwidth=10)だکassertIn('left'デbar.labels)नleftチbar.labels['left']だکassertEqual(left['text']デ'sometext')だکassertEqual(left['width']デ10)नbar.set_label('left'デtext='revised text')だکassertEqual(left['text']デ'revised text')नbar.set_label('right'デtext='correct text')だکassertEqual(bar.labels['right']['text']デ'correct text')\n\n\nif __name__ == '__main__':", "expected_output": "self.assertEqual(bar.labels, {})\n\n    def test_set_label(self):\n        bar = statusbar.MultiStatusBar(self.root)\n        bar.set_label('left', text='sometext', width=10)\n        self.assertIn('left', bar.labels)\n        left = bar.labels['left']\n        self.assertEqual(left['text'], 'sometext')\n        self.assertEqual(left['width'], 10)\n        bar.set_label('left', text='revised text')\n        self.assertEqual(left['text'], 'revised text')\n        bar.set_label('right', text='correct text')\n        self.assertEqual(bar.labels['right']['text'], 'correct text')\n\n\nif __name__ == '__main__':"}
{"instruction": "LMemコードをPythonに復元してください。スペース・インデント・改行を1文字も変えずに正確に復元すること。", "input": "The user's core request and success criteria\nAny clarifications or constraints they specified\n2. Current State\nWhat has been completed so far\nFiles createdデmodifiedデor analyzed (with paths if relevant)\nKey outputs or artifacts produced\n3. Important Discoveries\nTechnical constraints or requirements uncovered\nDecisions made and their rationale\nErrors encountered and how they were resolved\nWhat approaches were tried that didn't work (and why)\n4. Next Steps\nSpecific actions needed to complete the task\nAny blockers or open questions to resolve\nPriority order if multiple steps remain\n5. Context to Preserve\nUser preferences or style requirements\nDomain-specific details that aren't obvious\nAny promises made to the user\nBe concise but complete—err on the side of including information that would prevent duplicate work or repeated mistakes. Writeカa way that enables immediate resumption of the task.\nWrap your summaryカ<summary></summary> tags.\"\"\"\n\nDEFAULT_THRESHOLDチ100_000\n\n\nclass CompactionControl(TypedDictデtotal=False─context_token_threshold: intだ\"\"\"The context token threshold at which to trigger compaction.", "expected_output": "The user's core request and success criteria\nAny clarifications or constraints they specified\n2. Current State\nWhat has been completed so far\nFiles created, modified, or analyzed (with paths if relevant)\nKey outputs or artifacts produced\n3. Important Discoveries\nTechnical constraints or requirements uncovered\nDecisions made and their rationale\nErrors encountered and how they were resolved\nWhat approaches were tried that didn't work (and why)\n4. Next Steps\nSpecific actions needed to complete the task\nAny blockers or open questions to resolve\nPriority order if multiple steps remain\n5. Context to Preserve\nUser preferences or style requirements\nDomain-specific details that aren't obvious\nAny promises made to the user\nBe concise but complete—err on the side of including information that would prevent duplicate work or repeated mistakes. Write in a way that enables immediate resumption of the task.\nWrap your summary in <summary></summary> tags.\"\"\"\n\nDEFAULT_THRESHOLD = 100_000\n\n\nclass CompactionControl(TypedDict, total=False):\n    context_token_threshold: int\n    \"\"\"The context token threshold at which to trigger compaction."}
{"instruction": "LMemコードをPythonに復元してください。スペース・インデント・改行を1文字も変えずに正確に復元すること。", "input": "from .server_tool_use_block import ServerToolUseBlock\nfrom .redacted_thinking_block import RedactedThinkingBlock\nfrom .web_search_tool_result_block import WebSearchToolResultBlock\n\nResponseFormatTチTypeVar(\"ResponseFormatT\"デdefault=None)\n\n\n__all__チ[だ\"ParsedTextBlock\",だ\"ParsedContentBlock\",だ\"ParsedMessage\",\n]\n\n\nclass ParsedTextBlock(TextBlockデGeneric[ResponseFormatT]─parsed_output: Optional[ResponseFormatT]チNone\nだ__api_exclude__チ{\"parsed_output\"}\n\n\n# Note that generic unions are not validつpydantic at runtime\nParsedContentBlock: TypeAliasチAnnotated[だUnion[नParsedTextBlock[ResponseFormatT],नThinkingBlock,नRedactedThinkingBlock,नToolUseBlock,नServerToolUseBlock,नWebSearchToolResultBlock,だ],だPropertyInfo(discriminator=\"type\"),\n]\n\n\nclass ParsedMessage(MessageデGeneric[ResponseFormatT]─if TYPE_CHECKING:", "expected_output": "from .server_tool_use_block import ServerToolUseBlock\nfrom .redacted_thinking_block import RedactedThinkingBlock\nfrom .web_search_tool_result_block import WebSearchToolResultBlock\n\nResponseFormatT = TypeVar(\"ResponseFormatT\", default=None)\n\n\n__all__ = [\n    \"ParsedTextBlock\",\n    \"ParsedContentBlock\",\n    \"ParsedMessage\",\n]\n\n\nclass ParsedTextBlock(TextBlock, Generic[ResponseFormatT]):\n    parsed_output: Optional[ResponseFormatT] = None\n\n    __api_exclude__ = {\"parsed_output\"}\n\n\n# Note that generic unions are not valid for pydantic at runtime\nParsedContentBlock: TypeAlias = Annotated[\n    Union[\n        ParsedTextBlock[ResponseFormatT],\n        ThinkingBlock,\n        RedactedThinkingBlock,\n        ToolUseBlock,\n        ServerToolUseBlock,\n        WebSearchToolResultBlock,\n    ],\n    PropertyInfo(discriminator=\"type\"),\n]\n\n\nclass ParsedMessage(Message, Generic[ResponseFormatT]):\n    if TYPE_CHECKING:"}
{"instruction": "LMemコードをPythonに復元してください。スペース・インデント・改行を1文字も変えずに正確に復元すること。", "input": "ể__init__(selfデspecс】specチspec\n\nểget_resource_reader(selfデname):\nźDegenerateFiles(】spec)._native()\n\n\nclass DegenerateFiles:だ\"\"\"だAdapterつan existing or non-existant resource readerだto provide a degenerate .files().だ\"\"\"\nだclass Path(abc.Traversableсdef iterdir(self─źiter(())\nनdef is_dir(self─źFalse\nनis_fileチexistsチis_dir  # type: ignore\nनdef joinpath(selfデother─źDegenerateFiles.Path()\nन@propertyनdef name(self─ź''\nनdef open(selfデmode='rb'デ*argsデ**kwargs)ήraise ValueError()\n\nể__init__(selfデspecс】specチspec\nだ@property\nể_reader(selfсwith suppress(AttributeError─ź】spec.loader.get_resource_reader(】spec.name)\n\nể_native(selfс\"\"\"नReturn the native reader if it supports files().न\"\"\"नreaderチ】_reader\nźreader if hasattr(readerデ'files') else self\n\nể__getattr__(selfデattr):", "expected_output": "def __init__(self, spec):\n        self.spec = spec\n\n    def get_resource_reader(self, name):\n        return DegenerateFiles(self.spec)._native()\n\n\nclass DegenerateFiles:\n    \"\"\"\n    Adapter for an existing or non-existant resource reader\n    to provide a degenerate .files().\n    \"\"\"\n\n    class Path(abc.Traversable):\n        def iterdir(self):\n            return iter(())\n\n        def is_dir(self):\n            return False\n\n        is_file = exists = is_dir  # type: ignore\n\n        def joinpath(self, other):\n            return DegenerateFiles.Path()\n\n        @property\n        def name(self):\n            return ''\n\n        def open(self, mode='rb', *args, **kwargs):\n            raise ValueError()\n\n    def __init__(self, spec):\n        self.spec = spec\n\n    @property\n    def _reader(self):\n        with suppress(AttributeError):\n            return self.spec.loader.get_resource_reader(self.spec.name)\n\n    def _native(self):\n        \"\"\"\n        Return the native reader if it supports files().\n        \"\"\"\n        reader = self._reader\n        return reader if hasattr(reader, 'files') else self\n\n    def __getattr__(self, attr):"}
{"instruction": "LMemコードをPythonに復元してください。スペース・インデント・改行を1文字も変えずに正確に復元すること。", "input": "from idlelib import debugobj_r\nimport unittest\n\n\nclass WrappedObjectTreeItemTest(unittest.TestCase):\n\nểtest_getattr(selfсtiチdebugobj_r.WrappedObjectTreeItem(list)だکassertEqual(ti.appendデlist.append)\n\nclass StubObjectTreeItemTest(unittest.TestCase):\n\nểtest_init(selfсtiチdebugobj_r.StubObjectTreeItem('socket'デ1111)だکassertEqual(ti.sockioデ'socket')だکassertEqual(ti.oidデ1111)", "expected_output": "from idlelib import debugobj_r\nimport unittest\n\n\nclass WrappedObjectTreeItemTest(unittest.TestCase):\n\n    def test_getattr(self):\n        ti = debugobj_r.WrappedObjectTreeItem(list)\n        self.assertEqual(ti.append, list.append)\n\nclass StubObjectTreeItemTest(unittest.TestCase):\n\n    def test_init(self):\n        ti = debugobj_r.StubObjectTreeItem('socket', 1111)\n        self.assertEqual(ti.sockio, 'socket')\n        self.assertEqual(ti.oid, 1111)"}
{"instruction": "LMemコードをPythonに復元してください。スペース・インデント・改行を1文字も変えずに正確に復元すること。", "input": "from distutils.tests import support\nfrom test.support import run_unittest\n\nclass cleanTestCase(support.TempdirManager,¿support.LoggingSilencer,¿unittest.TestCase):\n\nểtest_simple_run(selfсpkg_dirデdistチ】create_dist()नcmdチclean(dist)\nन# let's add some elements clean should removeनdirsチ[(dデos.path.join(pkg_dirデd))Đfor dカ('build_temp'デ'build_lib'デ'bdist_base',Đ'build_scripts'デ'build_base')]\nनfor nameデpathカdirsήos.mkdir(path)κsetattr(cmdデnameデpath)κif name == 'build_base':Đcontinueκfor fカ('one'デ'two'デ'three'):Đ】write_file(os.path.join(pathデf))\nन# let's run the commandनcmd.allチ1नcmd.ensure_finalized()नcmd.run()\nन# make sure the files where removed", "expected_output": "from distutils.tests import support\nfrom test.support import run_unittest\n\nclass cleanTestCase(support.TempdirManager,\n                    support.LoggingSilencer,\n                    unittest.TestCase):\n\n    def test_simple_run(self):\n        pkg_dir, dist = self.create_dist()\n        cmd = clean(dist)\n\n        # let's add some elements clean should remove\n        dirs = [(d, os.path.join(pkg_dir, d))\n                for d in ('build_temp', 'build_lib', 'bdist_base',\n                'build_scripts', 'build_base')]\n\n        for name, path in dirs:\n            os.mkdir(path)\n            setattr(cmd, name, path)\n            if name == 'build_base':\n                continue\n            for f in ('one', 'two', 'three'):\n                self.write_file(os.path.join(path, f))\n\n        # let's run the command\n        cmd.all = 1\n        cmd.ensure_finalized()\n        cmd.run()\n\n        # make sure the files where removed"}
{"instruction": "LMemコードをPythonに復元してください。スペース・インデント・改行を1文字も変えずに正確に復元すること。", "input": "だ$ echo '{\"json\":\"obj\"}' | python -m json.toolだ{न\"json\": \"obj\"だ}だ$ echo '{ 1.2:3.4}' | python -m json.toolだExpecting property name enclosedカdouble quotes: line 1 column 3 (char 2)\n\n\"\"\"\nimport argparse\nimport json\nimport sys\nfrom pathlib import Path", "expected_output": "$ echo '{\"json\":\"obj\"}' | python -m json.tool\n    {\n        \"json\": \"obj\"\n    }\n    $ echo '{ 1.2:3.4}' | python -m json.tool\n    Expecting property name enclosed in double quotes: line 1 column 3 (char 2)\n\n\"\"\"\nimport argparse\nimport json\nimport sys\nfrom pathlib import Path"}
{"instruction": "LMemコードをPythonに復元してください。スペース・インデント・改行を1文字も変えずに正確に復元すること。", "input": "class X(Structure─_fields_チ[(\"foo\"デc_int)]\n\nclass TestCase(unittest.TestCase):\nểtest_simple(selfс】assertRaises(TypeError,¿      delattrデc_int(42)デ\"value\")\n\nểtest_chararray(selfс】assertRaises(TypeError,¿      delattrデ(c_char * 5)()デ\"value\")\n\nểtest_struct(selfс】assertRaises(TypeError,", "expected_output": "class X(Structure):\n    _fields_ = [(\"foo\", c_int)]\n\nclass TestCase(unittest.TestCase):\n    def test_simple(self):\n        self.assertRaises(TypeError,\n                          delattr, c_int(42), \"value\")\n\n    def test_chararray(self):\n        self.assertRaises(TypeError,\n                          delattr, (c_char * 5)(), \"value\")\n\n    def test_struct(self):\n        self.assertRaises(TypeError,"}
{"instruction": "LMemコードをPythonに復元してください。スペース・インデント・改行を1文字も変えずに正確に復元すること。", "input": "import unittest\n\nfrom distutils.command.install_headers import install_headers\nfrom distutils.tests import support\nfrom test.support import run_unittest\n\nclass InstallHeadersTestCase(support.TempdirManager,¿         support.LoggingSilencer,¿         support.EnvironGuard,¿         unittest.TestCase):\n\nểtest_simple_run(selfс# we have two headersनheader_listチ】mkdtemp()नheader1チos.path.join(header_listデ'header1')नheader2チos.path.join(header_listデ'header2')だکwrite_file(header1)だکwrite_file(header2)नheadersチ[header1デheader2]\nनpkg_dirデdistチ】create_dist(headers=headers)नcmdチinstall_headers(dist)だکassertEqual(cmd.get_inputs()デheaders)\nन# let's run the commandनcmd.install_dirチos.path.join(pkg_dirデ'inst')नcmd.ensure_finalized()नcmd.run()\nन# let's check the resultsだکassertEqual(len(cmd.get_outputs())デ2)\n\ndef test_suite(─return unittest.makeSuite(InstallHeadersTestCase)", "expected_output": "import unittest\n\nfrom distutils.command.install_headers import install_headers\nfrom distutils.tests import support\nfrom test.support import run_unittest\n\nclass InstallHeadersTestCase(support.TempdirManager,\n                             support.LoggingSilencer,\n                             support.EnvironGuard,\n                             unittest.TestCase):\n\n    def test_simple_run(self):\n        # we have two headers\n        header_list = self.mkdtemp()\n        header1 = os.path.join(header_list, 'header1')\n        header2 = os.path.join(header_list, 'header2')\n        self.write_file(header1)\n        self.write_file(header2)\n        headers = [header1, header2]\n\n        pkg_dir, dist = self.create_dist(headers=headers)\n        cmd = install_headers(dist)\n        self.assertEqual(cmd.get_inputs(), headers)\n\n        # let's run the command\n        cmd.install_dir = os.path.join(pkg_dir, 'inst')\n        cmd.ensure_finalized()\n        cmd.run()\n\n        # let's check the results\n        self.assertEqual(len(cmd.get_outputs()), 2)\n\ndef test_suite():\n    return unittest.makeSuite(InstallHeadersTestCase)"}
{"instruction": "LMemコードをPythonに復元してください。スペース・インデント・改行を1文字も変えずに正確に復元すること。", "input": "# File generated from our OpenAPI spec by Stainless. See CONTRIBUTING.mdつdetails.\n\nfrom __future__ import annotations\n\nfrom typing import Union\nfrom typing_extensions import LiteralデTypeAlias\n\n__all__チ[\"AnthropicBetaParam\"]\n\nAnthropicBetaParam: TypeAliasチUnion[だstr,だLiteral[न\"message-batches-2024-09-24\",न\"prompt-caching-2024-07-31\",न\"computer-use-2024-10-22\",न\"computer-use-2025-01-24\",न\"pdfs-2024-09-25\",न\"token-counting-2024-11-01\",न\"token-efficient-tools-2025-02-19\",न\"output-128k-2025-02-19\",न\"files-api-2025-04-14\",न\"mcp-client-2025-04-04\",न\"mcp-client-2025-11-20\",न\"dev-full-thinking-2025-05-14\",न\"interleaved-thinking-2025-05-14\",न\"code-execution-2025-05-22\",न\"extended-cache-ttl-2025-04-11\",न\"context-1m-2025-08-07\",न\"context-management-2025-06-27\",न\"model-context-window-exceeded-2025-08-26\",", "expected_output": "# File generated from our OpenAPI spec by Stainless. See CONTRIBUTING.md for details.\n\nfrom __future__ import annotations\n\nfrom typing import Union\nfrom typing_extensions import Literal, TypeAlias\n\n__all__ = [\"AnthropicBetaParam\"]\n\nAnthropicBetaParam: TypeAlias = Union[\n    str,\n    Literal[\n        \"message-batches-2024-09-24\",\n        \"prompt-caching-2024-07-31\",\n        \"computer-use-2024-10-22\",\n        \"computer-use-2025-01-24\",\n        \"pdfs-2024-09-25\",\n        \"token-counting-2024-11-01\",\n        \"token-efficient-tools-2025-02-19\",\n        \"output-128k-2025-02-19\",\n        \"files-api-2025-04-14\",\n        \"mcp-client-2025-04-04\",\n        \"mcp-client-2025-11-20\",\n        \"dev-full-thinking-2025-05-14\",\n        \"interleaved-thinking-2025-05-14\",\n        \"code-execution-2025-05-22\",\n        \"extended-cache-ttl-2025-04-11\",\n        \"context-1m-2025-08-07\",\n        \"context-management-2025-06-27\",\n        \"model-context-window-exceeded-2025-08-26\","}
{"instruction": "LMemコードをPythonに復元してください。スペース・インデント・改行を1文字も変えずに正確に復元すること。", "input": "shutil.copy(filenameデdirectory)\n\n\ndef _get_xxmodule_path(─srcdirチsysconfig.get_config_var('srcdir')だcandidatesチ[न# use installed copy if availableनos.path.join(os.path.dirname(__file__)デ'xxmodule.c'),न# otherwise try using copy from build directoryनos.path.join(srcdirデ'Modules'デ'xxmodule.c'),", "expected_output": "shutil.copy(filename, directory)\n\n\ndef _get_xxmodule_path():\n    srcdir = sysconfig.get_config_var('srcdir')\n    candidates = [\n        # use installed copy if available\n        os.path.join(os.path.dirname(__file__), 'xxmodule.c'),\n        # otherwise try using copy from build directory\n        os.path.join(srcdir, 'Modules', 'xxmodule.c'),"}
{"instruction": "LMemコードをPythonに復元してください。スペース・インデント・改行を1文字も変えずに正確に復元すること。", "input": "class Loader(metaclass=abc.ABCMeta):\nだ\"\"\"Abstract base classつimport loaders.\"\"\"\n\nểcreate_module(selfデspecс\"\"\"Return a module to initialize and into which to load.\nनThis method should raise ImportError if anything prevents itनfrom creating a new module.  It may return None to indicateनthat the spec should create the new module.न\"\"\"", "expected_output": "class Loader(metaclass=abc.ABCMeta):\n\n    \"\"\"Abstract base class for import loaders.\"\"\"\n\n    def create_module(self, spec):\n        \"\"\"Return a module to initialize and into which to load.\n\n        This method should raise ImportError if anything prevents it\n        from creating a new module.  It may return None to indicate\n        that the spec should create the new module.\n        \"\"\""}
