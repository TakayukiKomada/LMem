# コア依存
tiktoken>=0.7.0

# Anthropic API（実験用）
anthropic>=0.40.0

# LoRA学習（オプション: GPU環境のみ）
torch>=2.0.0
transformers>=4.45.0
peft>=0.12.0
trl>=0.12.0
datasets>=2.20.0
accelerate>=0.34.0
bitsandbytes>=0.43.0
